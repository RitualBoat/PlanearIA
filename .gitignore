# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# =============================================================================
# Dependencies
# =============================================================================
node_modules/

# Python virtual environment (local tooling only; never committed or scanned)
.venv/
venv/
__pycache__/
*.pyc

# =============================================================================
# Expo / React Native
# =============================================================================
.expo/
dist/
web-build/
expo-env.d.ts

# Generated native folders
/ios
/android

# Metro bundler
.metro-health-check*

# Native build artifacts
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# =============================================================================
# Environment & Secrets
# =============================================================================
.env
.env.*
.env*.local
!.env.example
!backend/.env.example
*.pem
devops_config.json
.vercel/


# =============================================================================
# AI Agents / Coding Assistants / Skills
# =============================================================================
# GitHub Copilot skills
skills/
skills-lock.json
.agents/*
.codex/*
roadmap-context
stitch-results

# Exceptions: shared agent workflows MUST be versioned because they configure the SDD workflow.
# The generic "skills/" pattern above also matches nested tool skill folders, so re-include the
# project-owned ones explicitly.
# NOTE: .agents/ is intentionally NOT re-included. It holds third-party design tools installed via
# `npx skills add ...` (e.g. impeccable): local, re-installable, and not versioned to keep the repo clean.
!.codex/config.toml
!.codex/skills/
!.codex/skills/**
!.claude/skills/
!.claude/skills/**

# Third-party design tool "impeccable" (npx skills add pbakaus/impeccable): the real files live under
# .agents/ (ignored above); Claude Code sees them through this symlink. Keep it local, do not commit it.
.claude/skills/impeccable
.impeccable.md

# Playwright MCP scratch output (page snapshots, traces)
.playwright-mcp/

# CodeGraph MCP local index (generated per machine; never commit the SQLite DB)
.codegraph/

# Local SQLite databases and sidecar files (device/export/debug only)
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
.mcp.local.json
.mcp.local.*.json


# AI coding tools (agent config directories)
.adal/
.agent/
.augment/
# .claude/ is NOT ignored — it holds shared Claude Code config (CLAUDE.md, settings.json, rules/)
.claude/settings.local.json
CLAUDE.local.md
.codebuddy/
.commandcode/
.continue/
.cortex/
.crush/
.factory/
.goose/
.iflow/
.junie/
.kilocode/
.kiro/
.kode/
.mcpjam/
.mux/
.neovate/
.openhands/
.pi/
.pochi/
.qoder/
.qwen/
.roo/
.trae/
.vibe/
.windsurf/
.zencoder/
.stitch-results/

# =============================================================================
# OpenSpec reference material (foreign template, study only, not active config)
# Keep Capturas/ tracked; ignore the cloned specboot template repo.
# =============================================================================
context/OpenSpec/Repo/

# =============================================================================
# IDE / Editor
# =============================================================================
.vscode/
.idea/

# =============================================================================
# Debug & Logs
# =============================================================================
npm-debug.*
yarn-debug.*
yarn-error.*

# =============================================================================
# OS Files
# =============================================================================
.DS_Store
Thumbs.db

# =============================================================================
# TypeScript
# =============================================================================
*.tsbuildinfo
.vercel

# External reference source snapshots are not PlanearIA implementation code.
context/referencias-opensource/*/source/

# Local externalized context backups - keep outside repo and never commit.
PLANEARIA_BACKUP_MOVE_OUT_*/
PlanearIA_Backup*/
context/stitch-results/
context/roadmap-context/
