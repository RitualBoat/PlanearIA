# Baseline Infraestructura - 2026-06-04

## Estado GitHub

- Classroom cerrado: issue #8 cerrado y en `Done`.
- Epic `Plan Maestro: Classroom / Grupos y Recursos` movida a `Done`.
- Epic `Plan Maestro: Infraestructura Local, CI y Deploy Basico` movida a `In progress`.
- Issue activo creado: #9 `Infraestructura Fase 0 - Auditoria operativa y baseline`.
- Milestone creado: `Ciclo 3 - Infraestructura Local y CI`.

## Inventario rapido

Frontend:

- React Native 0.81.5.
- Expo 54.0.34.
- TypeScript 5.9.2.
- Scripts principales: `start`, `start:dev`, `web`, `android`, `ios`, `lint`, `test`, `format`.

Backend:

- Carpeta: `backend/`.
- API serverless en `backend/api`.
- Scripts: `start` (`vercel dev`) y `deploy` (`vercel --prod`).
- Endpoints detectados: auth, sync, health, planeaciones, grupos, alumnos, recursos, plantillas, classroom/copiloto, entre otros.

CI:

- Workflow: `.github/workflows/ci.yml`.
- Jobs: TypeScript, ESLint, Jest.
- Triggers: push/pull_request contra `main` y `development`.

## Validaciones ejecutadas

- `npx tsc --noEmit`: OK.
- `npm run lint -- --quiet`: OK.
- `npm test -- --runInBand`: OK.

Resultado Jest:

- 73 test suites passed.
- 549 tests passed.

Warnings conocidos:

- `baseline-browser-mapping` desactualizado.
- Warnings esperados de `expo-notifications` en entorno de test.
- Warnings `act(...)` en algunos tests existentes.
- Logs esperados de pruebas de sync con errores simulados.

## Nota SQLite / Actividad Academica

La migracion AsyncStorage -> SQLite no se ejecuta en esta fase. Queda preparada en el plan como trabajo futuro con evidencia antes/despues:

- Capturas del flujo actual con AsyncStorage.
- Matriz de alternativas.
- Diagrama antes/despues.
- Evidencia de migracion y tests.
- Screenshots de app funcionando antes y despues.
